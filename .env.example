BOT_TOKEN=
# Local dev / Docker: use your Atlas URI or e.g. mongodb://mongo:27017/family-expenses if a mongo service exists
MONGODB_URI=mongodb://127.0.0.1:27017/family-expenses
APP_TIMEZONE=UTC

# Docker Compose (optional): host port → container 3000
# HOST_PORT=3000
